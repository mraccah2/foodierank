# Git history was scrubbed on 2026-07-03 (git filter-repo): all previously
# committed Google API keys were rotated, deleted in GCP, and redacted from
# every commit. No per-commit key fingerprints remain to allowlist.
#
# Add new entries here only for verified false positives, formatted as:
#   <commit-sha>:<file>:<rule>:<line>
