# Copy the relevant lines into android/local.properties (which is gitignored).
# Flutter normally generates flutter.sdk / sdk.dir for you; the only line you
# need to add by hand is the Google Maps API key used by the native manifest:

MAPS_API_KEY=YOUR_ANDROID_GOOGLE_MAPS_API_KEY
